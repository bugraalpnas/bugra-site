{
  "name": "bugra-site",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "node generate-posts.js && vite",
    "build": "node generate-posts.js && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.2.4"
  },
  "dependencies": {
    "decap-cms-app": "^3.9.0"
  }
}